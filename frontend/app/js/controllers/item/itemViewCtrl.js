if (typeof define === 'function' && define.amd) {
  define(['jquery'], function itemViewController($) {
    'use strict';

    return function callback() {
      return { a: 'dakhrohasnshfhfjsnf' };
    };
  });
}
